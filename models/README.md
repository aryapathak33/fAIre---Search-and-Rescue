# Models folder

Trained model weights are saved here locally but are not committed to git.

Typical output after training:

```text
models/fire_model.pt
models/fire_model.json
```

The `.pt` file is ignored because model checkpoints can become large. The metadata JSON can be shared if it does not include private data.
